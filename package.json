{
  "name": "root",
  "private": true,
  "scripts": {
    "deploy": "cd ./packages/service-users && sls package && sls deploy --aws-profile limawebdev"
  },
  "devDependencies": {
    "@shelf/jest-dynamodb": "^1.8.1",
    "lerna": "^4.0.0",
    "serverless": "2.38.0",
    "serverless-dynamodb-local": "^0.2.39",
    "serverless-webpack": "5.4.1"
  },
  "dependencies": {
    "aws-sdk": "2.892.0"
  }
}
